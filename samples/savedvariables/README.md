# Sample SavedVariables

Sanitized captures used as fixtures for companion development.

Before adding a file here, strip real account identifiers (the `#1`-style
account folder name), and replace character and realm names. Never commit an
unsanitized personal history file or an entire WTF directory.
